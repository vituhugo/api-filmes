<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listagem de generos</title>
</head>
<body>
    <table>
        <tr>
            <th>id</th>
            <th>nome</th>
            <th>ranking</th>
            <th>#</th>
        </tr>
        <% result.forEach((item) => { %>
        
        <tr>
            <td><%= item.id %></td>
            <td><%= item.nome %></td>
            <td><%= item.ranking %></td>
            <td><button>DELETAR</button></td>
        </tr>

        <% }) %>
    </table>
</body>
</html>